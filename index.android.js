import React, {
    AppRegistry,
    Component,
    StyleSheet,
    Text,
    View,
    TextInput
} from 'react-native';

class ReactNativeUI extends Component {
    render() {
        return (
            <View></View>
        );
    }
}

AppRegistry.registerComponent('ReactNativeUI', () => ReactNativeUI);
